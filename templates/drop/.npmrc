node-linker=hoisted

# Libs mutualisées @thomyiy/* depuis GitHub Packages.
# Exporter un token (read:packages) : export GITHUB_TOKEN=...
@thomyiy:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
